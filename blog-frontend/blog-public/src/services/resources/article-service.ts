import { resourceAxios } from "../../utils/axios-utils"

export const articleService = {
    // getArticles: async () => {
    //     const result = await resourceAxios.get<void, PageRespsone<>>('/articles',)
    // }

}